export enum Inputs {
  Name = 'name',
  Path = 'path',
  Retry = 'retry',
  RetryTimeout = 'retry_timeout',
  RetryInterval = 'retry_interval'
}
export enum Outputs {
  DownloadPath = 'download-path'
}
